{
  "name": "freeflow-server",
  "version": "1.0.0",
  "description": "Bun server for Freeflow - WebSocket hub, PTY manager, and file coordinator",
  "type": "module",
  "scripts": {
    "start": "bun --env-file=../.env run src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "latest"
  }
}
